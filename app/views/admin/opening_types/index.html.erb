<h1>Listing opening_types</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @opening_types.each do |opening_type| %>
  <tr>
    <td><%= opening_type.name %></td>
    <td><%= link_to 'Show', admin_opening_type_path(opening_type) %></td>
    <td><%= link_to 'Edit', edit_admin_opening_type_path(opening_type) %></td>
    <td><%= link_to 'Destroy', admin_opening_type_path(opening_type), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Opening type', new_admin_opening_type_path %>
